set daemon 60 with start delay 5 seconds
set mailserver localhost
set eventqueue
    basedir /var/monit
    slots 100
set logfile syslog
Include <%= node[:monit][:conf_dir] %>/*.monitrc
set httpd port 2812 and use the address localhost
  allow localhost
